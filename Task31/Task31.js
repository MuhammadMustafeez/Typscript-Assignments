"use strict";
let users = ["Muhammad", "Ali", "Sufyan", "Ahmad"];
// if test to check if the list of users is empty
if (users.length === 0) {
    console.log("We need to find some users!");
}
else {
    // Remove all usernames from the array
    users = [];
    console.log("All usernames have been removed.");
}
