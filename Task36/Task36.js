"use strict";
//performing a function for Shirt size and message.
function make_shirt(size, message) {
    console.log(`its size is ${size}.${message}`);
}
make_shirt("large", `It is fit for you. `);
